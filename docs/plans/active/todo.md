# Active TODO (Ordered)

1. [ ] Profile node-editor interaction hotspots and document top three latency contributors.
2. [ ] Implement one targeted responsiveness improvement that preserves visual behavior.
3. [ ] Add or update a deterministic benchmark/check that catches regressions in node-editor interaction latency.
4. [ ] Reduce dead-code warning surface in core runtime modules so checks stay signal-heavy for regressions.

Status notes (2026-02-25):
- Focus is on GUI node-editor responsiveness and documentation handoff hygiene.
- `scripts/run_agent_request.sh` is the mandatory preflight entrypoint for agent housekeeping requests.
